---
- name: "Definitive Timezone Diagnostic Playbook"
  hosts: localhost
  gather_facts: true
  connection: local

  tasks:
    - name: "Print payload"
      debug:
        msg: "payload: {{ full_payload }}"
